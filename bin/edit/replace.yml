trivially-true:
  search: |
    ((binary_expression (_) @left "==" (_) @right)
     (#eq? @left @right))
  replace: |
    true

if-true:
  search: |
    (if_expression (block_expression (_) @body))
  replace: |
    @body
